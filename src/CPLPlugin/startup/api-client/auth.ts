export const getJwtToken = (): string | null => {
  return null;
};

export const setJwtToken = (_token: string | null): void => {
  // Authentication is stored in the server-issued HttpOnly cookie.
};
